import React from "react";
import styles from './timepicker.module.css';

export default function TimePicker({ isAutoThemeEnabled }) {
  return (
    <div className={styles.container}>
      Time Picker
    </div>
  );
}
